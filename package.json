{
  "name": "sentinel",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "services/*",
    "libs/*"
  ],
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy --guided",
    "test": "jest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "demo": "ts-node scripts/demo-generate-events.ts",
    "replay-dlq": "ts-node scripts/replay-dlq.ts"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch": "^3.500.0",
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/client-eventbridge": "^3.500.0",
    "@aws-sdk/client-sqs": "^3.500.0",
    "@aws-sdk/lib-dynamodb": "^3.500.0",
    "@aws-sdk/util-dynamodb": "^3.500.0",
    "ajv": "^8.12.0",
    "aws-xray-sdk-core": "^3.5.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.122",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.30",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.2"
  }
}
